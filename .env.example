# Copy to .env.local and fill in.
# If left blank, the app falls back to a deterministic mock analyzer
# so the project is fully runnable without paid credentials.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
